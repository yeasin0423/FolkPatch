<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.APatch" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.APatch</item>
        <!-- Compat + platform splash attrs. Without these, API 31+ auto-uses the
             launcher icon (looks fine), but API 26-30 falls back to a black window
             with the generic APK placeholder icon in the center. -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/splash_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
    </style>

    <style name="Theme.APatch.WebUI" parent="Theme.APatch">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>

    <!-- Shizuku 授权确认界面：半透明背景对话框 -->
    <style name="Theme.APatch.Permission" parent="Theme.APatch">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>